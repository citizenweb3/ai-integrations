node_modules
.next
dist
coverage
.env
.env.local
sa.json
*.sa.json
.DS_Store
*.log
*.tsbuildinfo
.idea
.gitnexus

# Agent task diaries (local, never committed)
.tasks/

# Playwright MCP scratch snapshots (local debugging only)
.playwright-mcp/

# Local screenshot scratch (audit / ux-verify / stage-*-verify etc.)
*.png
